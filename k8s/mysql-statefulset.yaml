apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
  namespace: taxi-prod
  labels:
    app: mysql
spec:
  serviceName: mysql-service
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: taxi_services
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_USER
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_PASSWORD
          ports:
            - containerPort: 3306
              name: mysql
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - mysqladmin
                - ping
                - -h
                - localhost
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command:
                - mysqladmin
                - ping
                - -h
                - localhost
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
  volumeClaimTemplates:
    - metadata:
        name: mysql-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
        # storageClassName: gp2  # Uncomment for AWSapiVersion: v1
        #kind: Service
        #metadata:
        #  name: mysql-service
        #  namespace: taxi-prod
        #  labels:
        #    app: mysql
        #spec:
        #  selector:
        #    app: mysql
        #  ports:
        #  - port: 3306
        #    targetPort: 3306
        #    name: mysql
        #  clusterIP: None  # Headless service for StatefulSetapiVersion: apps/v1
        #kind: Deployment
        #metadata:
        #  name: taxi-backend
        #  namespace: taxi-prod
        #  labels:
        #    app: taxi-backend
        #    version: v1
        #spec:
        #  replicas: 2
        #  selector:
        #    matchLabels:
        #      app: taxi-backend
        #  strategy:
        #    type: RollingUpdate
        #    rollingUpdate:
        #      maxSurge: 1
        #      maxUnavailable: 0
        #  template:
        #    metadata:
        #      labels:
        #        app: taxi-backend
        #        version: v1
        #      annotations:
        #        prometheus.io/scrape: "true"
        #        prometheus.io/port: "8080"
        #        prometheus.io/path: "/actuator/prometheus"
        #    spec:
        #      initContainers:
        #      - name: wait-for-mysql
        #        image: busybox:1.36
        #        command:
        #        - sh
        #        - -c
        #        - |
        #          echo "Waiting for MySQL to be ready..."
        #          until nc -z mysql-service 3306; do
        #            echo "MySQL not ready yet..."
        #            sleep 5
        #          done
        #          echo "MySQL is ready!"
        #      containers:
        #      - name: taxi-backend
        #        image: rudrataxi-backend:v1
        #        imagePullPolicy: IfNotPresent
        #        ports:
        #        - containerPort: 8080
        #          name: http
        #          protocol: TCP
        #        envFrom:
        #        - configMapRef:
        #            name: backend-config
        #        - secretRef:
        #            name: db-secret
        #        env:
        #        - name: DB_URL
        #          valueFrom:
        #            secretKeyRef:
        #              name: db-secret
        #              key: DB_URL
        #        - name: DB_USER
        #          valueFrom:
        #            secretKeyRef:
        #              name: db-secret
        #              key: DB_USER
        #        - name: DB_PASSWORD
        #          valueFrom:
        #            secretKeyRef:
        #              name: db-secret
        #              key: DB_PASSWORD
        #        - name: JAVA_OPTS
        #          value: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0"
        #        volumeMounts:
        #        - name: uploads
        #          mountPath: /app/uploads
        #        resources:
        #          requests:
        #            memory: "512Mi"
        #            cpu: "250m"
        #          limits:
        #            memory: "1Gi"
        #            cpu: "500m"
        #        livenessProbe:
        #          httpGet:
        #            path: /actuator/health/liveness
        #            port: 8080
        #          initialDelaySeconds: 60
        #          periodSeconds: 15
        #          timeoutSeconds: 5
        #          failureThreshold: 3
        #        readinessProbe:
        #          httpGet:
        #            path: /actuator/health/readiness
        #            port: 8080
        #          initialDelaySeconds: 45
        #          periodSeconds: 10
        #          timeoutSeconds: 3
        #          failureThreshold: 3
        #        startupProbe:
        #          httpGet:
        #            path: /actuator/health
        #            port: 8080
        #          initialDelaySeconds: 30
        #          periodSeconds: 10
        #          failureThreshold: 30
        #      volumes:
        #      - name: uploads
        #        emptyDir: {}