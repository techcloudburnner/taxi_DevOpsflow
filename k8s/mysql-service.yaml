apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: taxi-prod
  labels:
    app: mysql
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  clusterIP: None  # Headless service for StatefulSet
