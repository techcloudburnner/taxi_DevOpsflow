apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: taxi-prod
  labels:
    app: prometheus
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      name: prometheus
  type: ClusterIP